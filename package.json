{
  "name": "norxstore",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p public && cp index.html public/ && mkdir -p dist && cp index.html dist/",
    "lint": "node -c server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "tesseract.js": "^7.0.0"
  }
}
